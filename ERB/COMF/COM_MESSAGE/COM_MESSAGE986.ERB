@COM_MESSAGE_986
;--------------------------------------------------------------------------
;デートに誘う（台詞は口上14、ここは結果の地の文のみ）
;--------------------------------------------------------------------------
IF !(TFLAG:成否用変数 == -2)
	PRINTFORML ＜デートに誘う＞
	PRINTFORML %CALLNAME:(TCVAR:MASTER:行為対象)%とデートすることになった。一緒に行きたい場所へ移動しよう。
ENDIF
