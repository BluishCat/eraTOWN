@COM_MESSAGE_996
;--------------------------------------------------------------------------
;同行してもらう
;--------------------------------------------------------------------------
IF !(TFLAG:成否用変数 == -2)
	PRINTFORML ＜同行してもらう＞
	PRINTFORML %CALLNAME:(TCVAR:MASTER:行為対象)%に一緒に行動してもらうことにした。
ENDIF
